package com.medtroniclabs.microcoaching.ai.voice

// Placeholder for a Bangla-specific STT contract. Superseded in practice by
// [OfflineSttEngine], which SherpaBengaliEngine (optional `sdk-android-sherpa`
// module) implements. Kept only so the name is not silently reused.
