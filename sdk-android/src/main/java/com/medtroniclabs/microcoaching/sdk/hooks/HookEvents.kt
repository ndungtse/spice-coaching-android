package com.medtroniclabs.microcoaching.sdk.hooks

// Placeholder for the internal event models a host adapter would pass into the
// decision engine (morning open, assessment ready, risk flag, visit completed,
// connectivity change). Nothing is defined yet — hosts currently call the
// concrete handlers in this package directly.
